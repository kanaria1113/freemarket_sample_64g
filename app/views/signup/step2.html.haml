.confirm
  = render 'registration/registration_header_active'
  .confirm-phone-number
    .confirm-phone-number__title
      電話番号の確認
    .confirm_phone_number__content
      .confirm-phone-number__content__input__title
        携帯電話の番号
      .confirm-phone-number__content__input__number
        = form_for @user, url: step3_signup_index_path,method: :get , html: {class: 'forth-main-wrapper__box'} do |f|
          = f.telephone_field :phone_number, placeholder: "携帯電話の番号を入力",class: "input_number"
          .confirm-phone-number__content__text
            本人確認のため、携帯電話のSMS(ショートメッセ
            ージサービス)を利用して認証を行います。
          .confirm-phone-number__content__send-btn
            = f.submit :SMSを送信する, class: "sms_btn"
      .confirm-phone-number__content__text
        *電話番号は本人確認や不正利用防止のために利用
        します。他のユーザーに公開されることはありま
        せん。   
      .confirm_phone_number__reason
        =link_to "#",class: "reason" do
          電話番号の確認が必要な理由>
  = render 'shared/user_footer'